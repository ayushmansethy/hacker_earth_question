for x in range(N):
    #     if s[x] != t[x]:
    #         ind = s.find(t[x], x)
    #         if ind != -1:

    #             # s[x] = s[ind]
    #             s=s.replace(s[x], s[ind], 1)
            